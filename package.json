{
    "name": "posty",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "rm -rf dist && tsc && vite build && ./fixdist.sh",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "^5.2.2",
        "vite": "^5.0.8",
        "@types/chrome": "^0.0.256"
    }
}
