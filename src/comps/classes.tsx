import { T_ClassInfo } from "../utils/types";

const classes = (classes: T_ClassInfo[]) => {
    return <div id="classes_cont"></div>;
};

const classes_cont = () => {
    return <div id="classes_cont"></div>;
};

export default classes;
